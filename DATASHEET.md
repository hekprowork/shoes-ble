# Smart Sole Sensor Node (智能鞋底感測模組) - 產品規格書 (Datasheet)

## 1. 產品概述 (Product Overview)

**Smart Sole Sensor Node** 是一款專為鞋底壓力與步態分析設計的微型無線感測模組。本模組內建高能效 32-bit RISC-V 核心微控制器，整合了兩組 FSR (薄膜壓力感測器) 以捕捉腳底受力變化，以及一組 MPU6050 (六軸慣性測量單元) 以記錄足部的加速度與角速度。所有感測資料皆透過高效率的 BLE (藍牙低功耗) 介面以專屬的二進位封包格式即時傳輸，適合各種運動科學與健康數據採集應用。

## 2. 腳位與硬體介面 (Hardware Interface)

### 2.1 核心規格
- **微控制器**: ESP32-C3 
- **核心架構**: 32-bit RISC-V SoC，支援 BLE 5.0

### 2.2 腳位定義 (Pinout)

| 元件名稱 | 訊號名稱 | 模組內部腳位 | 備註說明 |
|---|---|---|---|
| **FSR 1 (壓力感測器 1)** | Analog In | **GPIO 0** | ADC 輸入 (12-bit 解析度, 0~4095) |
| **FSR 2 (壓力感測器 2)** | Analog In | **GPIO 1** | ADC 輸入 (硬體反轉處理：4095 - 讀數) |
| **MPU6050 (六軸 IMU)** | SDA | **GPIO 7** | I2C 資料線 (I2C 位址: `0x68`) |
| **MPU6050 (六軸 IMU)** | SCL | **GPIO 6** | I2C 時脈線 |

## 3. BLE 藍牙通訊協定 (Communication Protocol)

為達到最佳傳輸效率並符合 BLE 單次 MTU 限制，本產品採用高度壓縮的 **16 Bytes 二進位封包** 進行資料傳輸。

### 3.1 GATT 服務定義
- **Device Name**: `ESP32-MPU6050-Tester`
- **Service UUID**: `12345678-1234-5678-1234-56789abcdef0`
- **Characteristic UUID**: `abcdef01-1234-5678-1234-56789abcdef0`
- **屬性**: READ, NOTIFY

### 3.2 封包資料格式 (16 Bytes Payload)
每次 Notify 皆發送 16 Bytes 的二進位資料。位元組順序採用 **Little Endian (小端序)**。

| 偏移量 (Byte) | 資料名稱 | 資料型別 | 數值意義與解析方式 |
|---|---|---|---|
| `0 ~ 1` | **FSR 1** | `uint16_t` (無號 16 位元) | 直接使用 (範圍: 0 ~ 4095) |
| `2 ~ 3` | **FSR 2** | `uint16_t` (無號 16 位元) | 直接使用 (範圍: 0 ~ 4095) |
| `4 ~ 5` | **ACC_X** | `int16_t` (有號 16 位元) | X 軸加速度。解析時需 **除以 100.0** (單位: m/s²) |
| `6 ~ 7` | **ACC_Y** | `int16_t` (有號 16 位元) | Y 軸加速度。解析時需 **除以 100.0** (單位: m/s²) |
| `8 ~ 9` | **ACC_Z** | `int16_t` (有號 16 位元) | Z 軸加速度。解析時需 **除以 100.0** (單位: m/s²) |
| `10 ~ 11` | **GYRO_X** | `int16_t` (有號 16 位元) | X 軸角速度。解析時需 **除以 100.0** (單位: rad/s) |
| `12 ~ 13` | **GYRO_Y** | `int16_t` (有號 16 位元) | Y 軸角速度。解析時需 **除以 100.0** (單位: rad/s) |
| `14 ~ 15` | **GYRO_Z** | `int16_t` (有號 16 位元) | Z 軸角速度。解析時需 **除以 100.0** (單位: rad/s) |

## 4. 軟體操作介面 (Python Receiver Application)

本模組附帶一個基於 Python 的電腦端接收與視覺化軟體，可即時連線並分析感測器數據。

### 4.1 軟體架構與核心套件
- **圖形介面 (GUI)**: 採用 `PySide6` 建構，提供跨平台的桌面應用程式體驗。
- **資料繪圖 (Plotting)**: 採用 `pyqtgraph` 進行高效能的 2D 即時折線圖繪製，並使用 `pyqtgraph.opengl` 處理 3D 姿態渲染。
- **藍牙通訊 (BLE)**: 採用 `bleak` 模組，透過 `qasync` 將非同步 (Asynchronous) 的 BLE 接收任務完美整合進 Qt 的事件迴圈中。
- **姿態解算 (Sensor Fusion)**: 內建演算法融合加速度計與陀螺儀數據，轉換成四元數 (Quaternion) 供 3D 姿態顯示。

### 4.2 核心功能與畫面配置

軟體介面主要分為三大區塊：

1. **FSR 壓力感測折線圖 (左上角)**
   - 即時顯示 FSR 1 與 FSR 2 的 ADC 數值變化。
   - 數值範圍介於 0 ~ 4095，X 軸顯示最新 500 筆歷史資料。
   - 可直觀判斷腳底（如前腳掌與後腳跟）的受力與踩踏狀態。

2. **IMU 慣性感測折線圖 (左下角)**
   - 同步顯示 MPU6050 傳回的 6 軸數據：包含三軸加速度 (Acc X, Y, Z) 與三軸角速度 (Gyro X, Y, Z)。
   - 提供實時的步態動態與動作劇烈程度參考。

3. **3D 姿態視覺化與狀態顯示 (右側)**
   - **連線狀態 (Status)**: 顯示目前藍牙掃描、連線中、或已斷線等即時狀態。
   - **3D 鞋底模型 (3D Posture View)**: 畫面中有一綠色長方體代表鞋底/感測器模組。軟體會根據 IMU 數據解算出的四元數 (Quaternion)，即時旋轉該 3D 模型，真實還原足部在空間中的翻轉、抬起等三維姿態。

### 4.3 安裝與操作流程

**步驟一：取得專案程式碼**
（若您不熟悉 Git，建議使用方式 A）
- **方式 A (一般下載)**：前往專案的 GitHub 網址，點選右上方綠色的「Code」按鈕，接著選擇「Download ZIP」。下載完成後，請將該 ZIP 檔解壓縮至您方便尋找的資料夾。
- **方式 B (透過 Git)**：在終端機輸入 `git clone <專案的_GitHub_網址>`

**步驟二：開啟終端機並進入專案目錄**
1. 打開電腦的終端機 (Mac 請搜尋「終端機」/ Terminal；Windows 請打開「命令提示字元」或 PowerShell)。
2. 使用 `cd` 指令切換至剛剛解壓縮的資料夾。
   *(小提示：對於不熟悉指令的用戶，您可以在終端機輸入 `cd ` (注意 cd 後面要有一個空白)，然後直接用滑鼠把剛剛解壓縮的「鞋底偵測」資料夾拖曳進終端機視窗，再按下 Enter 鍵即可！)*

**步驟三：連接與啟動軟體**
1. 確保電腦的藍牙功能已開啟。
2. 將 ESP32-C3 模組接上電源。
3. **執行 Python 程式**：在終端機輸入 `pixi run python main.py` 來啟動程式。（註：您的電腦需預先安裝好套件管理工具 `pixi`，它會自動幫您下載並設定所有需要的環境）。
4. 啟動後，軟體會自動於背景掃描名稱為 `ESP32-MPU6050-Tester` 的藍牙裝置 (掃描 5 秒)。
5. 找到裝置後會自動建立連線並訂閱特徵值 (Characteristic)，介面將立刻開始繪製即時數據與 3D 姿態模型。

